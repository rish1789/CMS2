# Backlog Progress

Tracks each feature's furthest completed stage in the `/speckit-orchestrate` pipeline (Specified → Clarified → Planned → Tasked → Analyzed → Implementing → Converged), in `build-order.md`'s order. Used to resume a batch run without re-deriving state.

| Feature | Status | Spec Dir | Notes |
|---|---|---|---|
| 001-clinic-registration | Converged | specs/001-clinic-registration | 30/37 tasks verified; 6 Testcontainers integration tests (T011–T016) + live quickstart (T035) written but unexecuted — sandbox-local Docker/docker-java bridge issue, not a code defect. Re-verify in a real dev/CI environment before treating as fully constitution-compliant (Principle I). |
| 039-patient-account-global-login | Converged | specs/002-patient-account-login | 24/31 tasks verified; 6 Testcontainers integration tests (T007–T011, T025) + live quickstart (T029) written but unexecuted — same sandbox-local Docker/docker-java bridge issue as 001, re-confirmed not a code defect. Found/fixed a real bug: `@SpringBootApplication` component scan didn't reach new sibling packages — relocated to `com.cms.CmsApplication`. Relocated `IndianMobileNumberValidator` to shared `com.cms.common` (T026); kept `PasswordPolicyValidator` intentionally duplicated per FR-008. |
| 002-super-admin-clinic-verification | Converged | specs/003-super-admin-verification | 17/22 tasks verified; 4 Testcontainers integration tests (T006–T008, T016) + live quickstart (T020) written but unexecuted — same sandbox Docker/docker-java issue as 001/002. Resolved a real backlog gap during Specify: no prior feature created a Super Admin identity — resolved as config-bootstrapped credentials (HTTP Basic Auth), no DB row, per user's explicit choice. De-verification cascade trigger implemented as a Spring ApplicationEvent (`ClinicDeVerifiedEvent`) for 008 to consume later, not a forward call into unbuilt code. Found/fixed a real bug: service was loading all clinics into memory to filter client-side instead of querying with WHERE — added `ClinicRepository.findByVerified()`. |
| 004-staff-onboarding-direct-hire | Converged | specs/004-staff-onboarding-direct-hire | 24/34 tasks verified; 9 Testcontainers integration tests + live quickstart (T032) written but unexecuted — same sandbox Docker issue. Resolved two build-order tensions during Specify: (1) pulled forward a minimal ClinicAdmin email+password login (STAFF-audience JWT) since 003-staff-login is scoped after this feature; (2) this feature owns creating the Doctor Profile row, 005 owns the verification workflow on top of it. Added a TemporaryPasswordGenerator utility (a genuine gap analyze caught — neither existing validator/encoder actually generates a compliant password). Found/fixed two cross-cutting bugs touching 001/002's code: a JWT filter wrongly auto-registered into unrelated @WebMvcTest slices, and a SecurityConfig constructor-signature break in 001's contract test — both re-verified passing after fixes. |
| 007-last-active-clinicadmin-protection | Converged | specs/005-last-active-clinicadmin-protection | 13/21 tasks verified; 7 Testcontainers integration tests + live quickstart (T019) written but unexecuted — same sandbox Docker issue. Resolved a real gap during Specify: no prior feature built any staff-deactivation capability at all, so this feature builds it, with the last-active-ClinicAdmin protection as its core rule. Found/fixed a real pre-ship security bug: the new deactivate endpoint would have been publicly accessible with no authentication (SecurityConfig's authorizeHttpRequests only explicitly protected onboarding, deactivate fell through to permitAll) — fixed with an explicit matcher, independently verified. |
| 003-staff-login-password-or-code | Converged | specs/006-staff-login-dual-identifier | 6/10 tasks verified; 3 Testcontainers integration tests + live quickstart (T009) written but unexecuted — same sandbox Docker issue. Narrow, clean extension of 004's pulled-forward login (added the staff-code alternate identifier) — no bugs found. Correctly handled the breaking field rename (email→identifier) by updating 004's pre-existing StaffLoginTest.java in the same pass, confirmed no regression. |
| 005-doctor-profile-auto-creation-license-queue | Converged | specs/007-doctor-profile-license-queue | 21/37 tasks verified (all implementation + the one Docker-independent test); 16 Testcontainers integration tests + quickstart validation written/compiling but unexecuted — same sandbox Docker/docker-java bridge issue as 001–006, re-confirmed this session. Converge found 0 actionable findings (all FR/SC/plan touch-points confirmed present and correct by code inspection). Multi-clinic dedup resolved with the user: match by license number cross-checked against specialization (case-insensitive, trimmed); reuse skips new credentials (login is Account-scoped); mismatch rejected 409; licenseVerified carries over on reuse. Added `visible` column + `uq_doctor_profile_license_number` constraint (V4), extended `StaffOnboardingService`'s Doctor branch for dedup/reuse (no regression: `StaffOnboardingContractTest` still passes), added `DoctorVerificationController/Service` mirroring 003, and `DoctorProfileRepository.findDiscoveryEligible()` gating on licenseVerified+visible+active RoleAssignment+clinic.verified (added mid-build via analyze fix I1). Full backend `build` (incl. spotless) and full frontend suite (40/40 incl. new tests) both green. **Post-convergence fix (2026-09-09):** `reuseExistingDoctor()` inserted a new active RoleAssignment on every re-onboard with no existence check, and `role_assignment` had no uniqueness constraint at all — re-onboarding the same doctor at the same clinic created two active rows for one Account, which also broke `StaffDeactivationService.findByAccount_IdAndClinic_Id` (a singular `Optional`, throws `IncorrectResultSizeDataAccessException` on >1 match — Deactivate would 500). Fixed with a partial unique index `uq_role_assignment_account_clinic_role_active` (V25) plus an `existsBy...ActiveTrue` guard in `reuseExistingDoctor` (idempotent no-op on duplicate re-onboard); pre-existing duplicate row cleaned up in dev DB. Re-verify the 16 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Multi-clinic dedup resolved with the user: match by license number cross-checked against specialization (case-insensitive, trimmed); a match reuses the existing Account/Doctor Profile with no new credentials (login is Account-scoped, not clinic-scoped); mismatch rejected as 409; licenseVerified carries over unchanged on reuse (only 006's license edit resets it). Plan reuses 003's verification pattern verbatim (no new module/security chain) and adds a DB-level `uq_doctor_profile_license_number` constraint per Constitution IV. |
| 006-doctor-license-edit-reverification-reset | Converged | specs/008-doctor-license-reverification-reset | 15/23 tasks verified (all implementation + the one Docker-independent test); 8 Testcontainers integration tests + quickstart validation written/compiling but unexecuted — same sandbox Docker limitation as 001–007. Converge found 0 actionable findings. No prior feature built any Doctor Profile edit capability at all — resolved with the user: Super Admin only (avoids multi-clinic ownership ambiguity, since the profile is global), and the edit action also closes 007's deferred `visible`-write gap. No migration needed (reused 007's schema entirely). Reset logic implemented as a plain in-transaction conditional (deliberately not an event) to structurally guarantee independence from 008-deverification-cascade's cascade. Full backend `build` (incl. spotless) and full frontend suite (41/41 incl. new tests) both green. Re-verify the 8 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | No prior feature built any Doctor Profile edit capability — resolved with the user during Specify: Super Admin only (avoids the multi-clinic ownership ambiguity a ClinicAdmin-edits model would create, since the profile is global). Also resolved during Clarify: the edit action includes 007's previously-unwritable `visible` toggle, closing that deferred gap. Caught and fixed a factual error during Specify: the source backlog text listed "contact info" as a DoctorProfile field; it actually lives on Account. Plan needs zero migrations — reuses 007's schema/constraint entirely; reset implemented as a plain in-transaction conditional (not an event) to structurally guarantee independence from 008-deverification-cascade's cascade. |
| 019-patient-record-auto-creation-phone-linking | Converged | specs/009-patient-record-phone-linking | 8/18 tasks verified (all implementation + regression checks); 10 Testcontainers integration tests + quickstart validation written/compiling but unexecuted — same sandbox Docker limitation as 001–008. Hit and fixed the known OneDrive `backend/build` corruption twice this session (`rm -rf backend/build`). Defined the clinic-scoped `Patient` entity for the first time (new `com.cms.patient.record` package), with two separate partial-unique DB constraints (not one flat one) closing two independent race conditions while protecting a linked record from a phone-sharing collision with a different account. Ships as a service-only contract (`PatientLinkingService`), no HTTP endpoint — its only consumers (017/018) don't exist yet. Converge found 1 real bug: the race-catch block caught any `DataAccessException` instead of narrowing to the specific constraint, unlike every other race-handling service in this codebase — fixed. Full backend `build` (incl. spotless) green, no regressions. Re-verify the 10 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | First feature to need a clinic-scoped Patient entity at all (no backlog file specifies its schema; 016/018/020 come later and each independently add their own walk-in creation against the same table). Ships with no HTTP endpoint — its only consumers (017/018) don't exist yet; service-interface-only contract per Constitution III, mirroring 007 US3's precedent. Resolved with the user during Clarify: a Patient record already linked to a different account is protected from phone-matching (privacy), which required catching and fixing a real design bug in the same pass — a flat clinic+phone uniqueness constraint would have silently broken the same-account race guarantee; resolved with two separate partial-unique constraints instead. |
| 035-public-discovery-search | Converged | specs/010-public-discovery-search | 25/25 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 11 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–009, re-confirmed this session (`IllegalStateException: Previous attempts to find a Docker environment failed`). Hit and fixed the known OneDrive `backend/build` corruption once this session (`rm -rf backend/build`). Reused 007's `DoctorProfileRepository.findDiscoveryEligible()` design intent rather than its exact method — built one new JPQL query (`DiscoveryResultRepository.search`, new `com.cms.discovery` module) driven from `RoleAssignment` so each result row carries the specific verified clinic that made a (possibly multi-clinic) doctor eligible, which `findDiscoveryEligible()`'s bare-`DoctorProfile` return type can't express. New public `GET /api/v1/discovery/search` endpoint, its own `SecurityFilterChain` (`@Order(5)`, `permitAll()`), no new migration (pure read over existing 001/004/005 tables). Ships with a frontend page (`DiscoverySearch.tsx`) in the same feature — unlike 009's service-only precedent — since this feature's consumer (a public visitor) exists right now, matching `clinic-registration`/`patient-account`'s pattern. Converge found 0 actionable findings. Full frontend suite (44/44 incl. 3 new discovery tests) green, no regressions. Re-verify the 11 unexecuted backend tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 036-notification-event-pipeline-opt-in-out | Converged | specs/011-notification-event-pipeline | 22/22 tasks verified (all implementation + full backend build incl. spotless, green); 8 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–010, re-confirmed this session. Per build-order.md's own documented resolution for this feature, scoped to the generic pipeline only (no fabricated booking/waitlist/follow-up-reminder call-sites, since none of 016/017/018/029 or any follow-up-reminder feature exists yet anywhere in the 39-feature backlog) — ships as a service-only contract (`NotificationEventService`), no HTTP endpoint, no frontend, mirroring 009's precedent. New `com.cms.notification` module: `NotificationEvent` entity (opaque `eventType`, snapshotted push/SMS eligibility, optional expiry window, PENDING/ACTIONED/EXPIRED status) + one migration (`V6`) additively extending `PatientAccount` with independent `smsOptIn`/`pushOptIn` fields (039's existing `notificationOptIn` left untouched — zero consumers, no regression risk to its converged, tested contract). Converge (first pass) found 1 real HIGH bug: `markActioned` was a read-then-check-then-write, so a concurrent `expireDue()` sweep could be silently overwritten back to `ACTIONED` — fixed by making it a data-layer-guarded conditional `@Modifying` update mirroring `expireDue`'s own design (Constitution Principle IV); re-verified via a second converge pass, 0 further findings. No frontend changes — full frontend suite (44/44) re-confirmed unaffected. Re-verify the 8 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 037-notification-delivery-stub | Converged | specs/012-notification-delivery-stub | 11/11 tasks verified (all implementation + full backend build incl. spotless, green); 4 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–011, re-confirmed this session. Genuinely wires into 036 (unlike 036 itself, which deliberately deferred wiring into its own not-yet-built callers) — 036's `publish()` was already converged and its FR-001 forbids waiting on a delivery step, so this feature connects via a plain Spring application event (`NotificationEventPublishedEvent`) plus a `@TransactionalEventListener(phase = AFTER_COMMIT)` listener, mirroring this codebase's own existing `ClinicDeVerifiedEvent` precedent (003 publishes, 008 later listens) exactly. `NotificationSender` interface + `LoggingNotificationSender` (its only v1 impl, log-only, zero config, zero network calls) makes a future real provider a drop-in replacement per the source material's stated design intent. No new migration, no new NotificationEvent field/status — delivery-status tracking explicitly out of scope. Test fixture registers a recording `NotificationSender` test double (`@TestConfiguration`/`@Primary`) so tests assert exact send calls without log-scraping. Converge found 0 actionable findings. Full frontend suite re-confirmed unaffected (44/44; one unrelated flaky RegistrationForm test passed on retry in isolation — pre-existing environmental flakiness, not caused by this backend-only feature). Re-verify the 4 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 009-recurring-schedule-definition | Converged | specs/013-recurring-schedule-definition | 24/24 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 12 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–012, re-confirmed this session. Hit and fixed the known OneDrive `backend/build` corruption once this session (`rm -rf backend/build`). New `com.cms.scheduling` module (Constitution III's own named example of a module boundary) with `Schedule` entity (`@ElementCollection<DayOfWeek>`, mode-conditional `slotIntervalMinutes`), reusing 004's existing `RoleAssignmentRepository.existsByAccount_IdAndClinic_IdAndRoleAndActiveTrue` for both authorization (ClinicAdmin-or-doctor-self) and the doctor-clinic staffing gate — no new authorization mechanism. Extends the existing `/api/v1/clinics/**` security chain (2 new matchers) rather than a new chain. Deliberately scoped to create+list only — overlap blocking (010) and non-retroactive edit (014) explicitly deferred per build-order.md's own documented placement of those features after this one; no Session/Slot entity exists yet anywhere (011 not built), so FR-011/SC-004 hold structurally. Ships with a frontend form (`ScheduleForm.tsx`), unlike 011/012's service-only precedent, since its callers (ClinicAdmin, Doctor) are real and already logged in today. Converge found 0 actionable findings. Re-verify the 12 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 010-multi-clinic-doctor-schedule-overlap-block | Converged | specs/014-schedule-overlap-block | 10/10 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 7 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–013, re-confirmed this session. Hit and fixed the known OneDrive `backend/build` corruption once this session (`rm -rf backend/build`). Pure additive extension of 009's `ScheduleService.create()` — one new repository query (`findByDoctorProfile_Id`, not clinic-scoped, so it catches cross-clinic overlaps), one new check step (shared day-of-week AND half-open-interval time overlap) run after 009's existing validation/staffing-gate checks so error precedence is unchanged, one new `409 SCHEDULE_OVERLAP` exception. Deliberately no travel-time/gap buffer or configuration (explicit, accepted v1 limitation per the backlog source, not an oversight). No new entity, no migration, no new endpoint. Converge found 0 actionable findings. | |
| 011-nightly-rolling-session-generation | Converged | specs/015-nightly-session-generation | 18/18 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 12 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–014, re-confirmed this session. Hit and fixed the known OneDrive `backend/build` corruption twice this session (`rm -rf backend/build`). Defined the `Session` entity for the first time (extends 009/010's `com.cms.scheduling` module) — snapshots mode/time-range/slot-interval from its parent Schedule at generation time (immune to a later Schedule edit, structurally supporting 014-schedule-edit-non-retroactivity even though that feature isn't built). Deliberately creates Session rows only — no Slot entity/logic, explicitly deferred to 012/013 per build-order.md's placement of both strictly after this feature. `uq_session_schedule_date` DB constraint plus per-schedule-own-transaction generation closes the duplicate-creation race (Constitution IV) without inventing SAVEPOINT-based row recovery this codebase has no precedent for. New real `@Scheduled` nightly cron (first in this codebase; added `@EnableScheduling`) plus a Super-Admin-only manual re-trigger endpoint reusing 003's existing admin auth chain unchanged. Ships a minimal Super Admin frontend trigger, following `PendingClinicsList`'s established `AdminCredentials`/Basic-Auth pattern. Converge (first pass) found 1 real HIGH bug: `generate()`'s per-schedule loop had no exception handling, so one Schedule's failure (e.g. the documented rare-race case) would silently abort generation for every schedule after it in iteration order — contradicting the feature's own documented per-schedule-isolation design; fixed by wrapping each schedule's generation in a try/catch that logs and continues. Also fixed a real frontend bug found during its own test run (not converge): the Super Admin sign-in form didn't display the error message when re-prompted after a 401, so the invalid-credentials test initially failed — fixed by rendering the error alert in both the signed-out and signed-in branches. Re-verified via a second converge pass, 0 further findings. Re-verify the 12 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 014-schedule-edit-non-retroactivity | Converged | specs/016-schedule-edit-non-retroactivity | 13/13 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 5 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–015, re-confirmed this session. Adds `PATCH .../schedules/{scheduleId}` to 013's module — reuses 009's validation and 010's overlap check (extended with a self-exclusion parameter) rather than duplicating either. Non-retroactivity is structural, not merely tested: `ScheduleService`/`edit()` never references `SessionRepository`/`Session` anywhere in its code path, verified by grep during convergence review. Caught and fixed a real security gap during implementation (before any test ran): the new PATCH path (`.../schedules/{id}`, one segment longer than 013's existing POST/GET matchers) didn't match any explicit `SecurityConfig` matcher and would have silently fallen through to `anyRequest().permitAll()`, making schedule edits publicly accessible with no authentication — fixed by adding an explicit matcher before implementation proceeded. Doctor/clinic reassignment deliberately excluded from edit scope (already satisfied structurally by 011's existing Session-snapshot design regardless). Converge found 0 further findings. Re-verify the 5 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 015-fee-resolution-and-locking | Converged | specs/017-fee-resolution-locking | 20/20 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 5 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–016, re-confirmed this session (one transient OneDrive file-lock on `build/libs/*.jar` during cleanup, resolved with a short retry — distinct from, but same root cause as, the recurring `backend/build` corruption). New `com.cms.booking` module (the backlog's own listed module, distinct from `com.cms.scheduling`) — `AppointmentType` (doctor-scoped, optional fee override) + `DoctorDefaultFee` (at most one per doctor), `FeeResolutionService.resolve()` implementing the exact override→default→hard-block order with no HTTP endpoint (016/017/018 aren't built yet — service-only contract per Constitution III, mirroring 009's precedent), plus a configuration surface (`AppointmentTypeService`/`BookingController`) authorized via "the doctor themselves, or an active ClinicAdmin at any clinic that doctor is staffed at" (one new `RoleAssignmentRepository` query). New `/api/v1/doctors/**` security chain (`@Order(6)`, reusing existing staff-JWT machinery) since this doctor-scoped (not clinic-scoped) path doesn't overlap any existing chain. No Booking entity, no payment-status, no frontend — all explicitly deferred to 016/017/018 or a future configuration-UI feature. First use of `BigDecimal` for currency anywhere in this codebase. Converge found 0 actionable findings. | |
| 012-fixed-time-slot-pregeneration | Converged | specs/018-fixed-time-slot-pregeneration | 14/14 tasks verified (all implementation + full backend build incl. spotless + full frontend suite unaffected, both green); 4 new Testcontainers integration tests written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–017, re-confirmed this session (one recurring OneDrive `backend/build` corruption hit and fixed via `rm -rf backend/build`). Defines `Slot` for the first time (extends 011/013/015's `com.cms.scheduling` module) — `SlotStatus` intentionally defined with only its one currently-meaningful value (`OPEN`), extended in place by future features exactly when they need to, mirroring `ScheduleMode`/`NotificationEventStatus`'s precedent. Resolves build-order.md's documented 012↔022 circular-dependency by implementing `BufferSlotCalculator` as a named, swappable interface whose only v1 bean (`ColdStartBufferSlotCalculator`) is a literal transcription of 022's own already-specified "fewer than 5 data points → 1 buffer slot" cold-start rule (unconditionally true today, since 021's no-show data doesn't exist) — 022 will supply a different implementation later with zero changes to slot-creation/even-distribution logic. Buffer placement uses a standard N-points-evenly-spaced-among-M-items formula, verified against both a 16-slot and a degenerate 1-slot case. Slot generation is hooked directly into 011's existing `generateForSchedule`, in the same per-schedule transaction, firing only for newly-created Fixed-Time Sessions (never Queue/Token, never on a repeat/already-generated Session) — no separate pass, no HTTP endpoint (016/017/018/023 aren't built yet). Converge found 0 actionable findings. | |
| 013-queue-mode-slot-on-demand-generation | Converged | specs/019-queue-slot-on-demand-generation | 10/10 tasks verified (all implementation + full backend build incl. spotless + full frontend suite unaffected, both green); 3 new Testcontainers integration tests (including a genuine 20-thread concurrency test) written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–018, re-confirmed this session. Extends 012's `Slot` entity in place (nullable `startTime`/`endTime`, new nullable `tokenNumber`) rather than a parallel Queue-specific entity — mirrors this backlog's established "extend in place" pattern. `QueueSlotService.issueNextSlot()` has no automatic trigger (018, its only real caller, is unbuilt) — a directly-callable service-only contract per Constitution III, mirroring 009/011's precedent. Never-reused token numbers closed at the data layer via a partial unique index `(session_id, token_number) WHERE token_number IS NOT NULL`, combined with 011's own proven non-transactional-outer/fresh-transaction-per-attempt retry pattern — deliberately more proactive than 011's "log and skip" recovery, since concurrent same-session bookings are a realistic scenario here, not a rare edge case, and every racing caller has an equally legitimate claim to a new token. No delay-figure tracking (queue position, 024, is the analogous not-yet-built concept). Converge found 0 actionable findings. | |
| 016-staff-assisted-fixed-time-booking | Converged | specs/020-staff-assisted-fixed-time-booking | 25/25 tasks verified (all implementation + full backend build incl. spotless + full frontend suite, both green); 7 new Testcontainers integration tests (including a genuine 10-thread concurrency test) written/compiling but unexecuted — same sandbox Docker/docker-java bridge limitation as 001–019, re-confirmed this session. Defines `Booking` for the first time (extends 015's `com.cms.booking` module) and adds `BOOKED` to 012's `SlotStatus` — the first real consumer of the whole Scheduling/Fee-Resolution pipeline built earlier this session (Slot → FeeResolutionService → Patient → Booking). Walk-in Patient creation reuses 009's existing `Patient` entity directly (its already-nullable `patientAccount` field was designed for exactly this case) rather than repurposing 009's account-keyed `PatientLinkingService`. Fee resolution is the first real write-gate in the flow (nothing is written before it succeeds) — one `@Transactional` method, ordering as the actual guarantee. One-Booking-per-Slot closed via a DB unique constraint on `booking.slot_id`, backed by an application-level `status == OPEN` fast-path check. Extended the existing `/api/v1/clinics/**` security chain with one explicit matcher (proactively avoiding the exact unauthenticated-endpoint gap class caught and fixed in 014 earlier this session). Ships a minimal staff-facing frontend form, consistent with 009/013's "real actor gets a real form" precedent. Converge (first pass) found 1 real HIGH bug: the concurrent-booking race-closure was documented but not actually reachable in code — `bookingRepository.save(booking)` (not `saveAndFlush`) defers the INSERT to transaction commit, by which point the surrounding try/catch had already returned, so a real race would have surfaced as an unhandled 500 instead of the tested `409 SLOT_ALREADY_BOOKED` — fixed by switching to `saveAndFlush`, forcing the constraint check synchronously inside the catch block. Re-verified via a second converge pass, 0 further findings. Also fixed a genuine coverage gap caught during analyze (before implementation): 015's `FeeResolutionService` exceptions had never been HTTP-mapped since 015 itself had no endpoint — this feature is their first HTTP-reachable caller, so `BookingExceptionHandler` needed two additional mappings the original task list omitted. Re-verify the 7 unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | |
| 017-patient-self-service-fixed-time-booking | Converged | specs/021-patient-self-service-booking | 24/24 tasks complete; full backend build (spotless + compile) and full frontend suite (57/57) both green with zero regressions. quickstart.md Scenarios 1-6 verified at the unit-of-behavior level; execution is blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct test run: `IllegalStateException: Could not find a valid Docker environment` — same root cause documented for every integration-tested feature this session). 10 new backend integration tests (incl. two concurrency tests: 020's proven Slot-booking race-closure pattern, and a new one proving 019's Patient-creation race-closure) written/compiling but unexecuted for that reason; 6 new frontend tests executed and passing. Clarified during spec-kit intake (user chose Option B over the recommended default): this feature also ships a new patient-facing "list open Slots" endpoint/UI, not just booking-by-known-slot-id — a larger scope than 016's own precedent. Discovered and closed two structural gaps neither prior spec anticipated: (1) `com.cms.patient.account.SecurityConfig` had zero authenticated endpoints and no JWT filter at all before this feature — added `PatientJwtAuthenticationFilter`/`PatientAuthenticationEntryPoint` mirroring 004's staff equivalents; (2) `PatientAccount` (039) has no name field and `LoginForm.tsx` never persisted its own login token — added a `patientName` field to the booking request (used only when `PatientLinkingService.findOrCreatePatient` actually creates a new record) and a new `patient-account/token.ts` (039's module) so any patient-authenticated frontend feature can obtain a token at all. Reuses 015's `FeeResolutionService`, 016's `Booking`/`saveAndFlush` race-closure pattern, and 019's `PatientLinkingService.findOrCreatePatient` as its documented contract — but converge (first pass) found the reuse exposed a genuine, previously-latent CRITICAL bug **in 019's own code**: `findOrCreatePatient`'s create-new branch used plain `save()` instead of `saveAndFlush()`, so its `uq_patient_clinic_account` race-closure catch block could never actually fire — the exact same bug class as 020's own `StaffBookingService` fix, just in a different, already-converged feature that had never had a real concurrency-exposed HTTP caller until now. Fixed in `PatientLinkingService.java`, covered by a new concurrent-first-booking regression test, re-verified via a second converge pass with 0 further findings. `/speckit-analyze` also found 2 LOW findings pre-implementation (missing not-found/mismatch test coverage; plan.md not listing the token.ts touch-point) — both remediated before implementation began. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). |
| 018-queue-token-booking | Converged | specs/022-queue-token-booking | 26/26 tasks complete; full backend build (spotless+compile) and full frontend suite (61/61, one unrelated pre-existing test flaked once under full-suite load and passed on isolated re-run) both green with zero regressions. 8 new backend integration tests (incl. a 10-thread concurrency test) written/compiling but unexecuted — re-confirmed via direct run (`IllegalStateException: Could not find a valid Docker environment`), same sandbox limitation as every prior feature. Clarified during spec-kit intake (user chose Option A): no session-browsing capability, book by known Session ID only — deferred pending 024-queue-position-tracking, unlike 021's Slot-list decision. The key technical theme this feature surfaced: `QueueSlotService.issueNextSlot` (013) calls its own `attemptIssueSlot` via self-invocation, silently bypassing Spring's `@Transactional` proxy on that inner method — harmless for `issueNextSlot` standalone (each `save()` becomes its own auto-committing transaction, which its retry-loop actually needs), but would silently defeat that retry closure if wrapped in a broader caller transaction. `StaffQueueBookingService`/`PatientQueueBookingService` are therefore deliberately NOT `@Transactional` at the top level (avoiding that class of bug proactively, unlike 020/021's own reactive fixes). Ironically, the first implementation then *reintroduced the identical self-invocation pattern* in its own new code — a `createBooking(...)` helper marked `@Transactional` but called via self-invocation from `bookSlot`, making its re-fetch-for-safety design (from `/speckit-analyze`'s own MEDIUM finding) silently ineffective. Caught by convergence (first pass) precisely because writing up F1 forced re-reading this feature's own code with the same scrutiny just applied to `QueueSlotService`; fixed by simplifying — no separate helper, no `@Transactional`, direct inline `bookingRepository.save(...)` using the original (correctly-fine-to-use-detached) `Slot` reference. Re-verified via a second converge pass, 0 further findings. Reuses 013's `QueueSlotService`, 015's `FeeResolutionService`, 016's `Booking`/staff-auth pattern, 019/021's `PatientLinkingService`, and 021's Patient-auth-filter chain entirely as-is. New response type `QueueBookingResponse` (not a reuse of `BookingResponse`) adds `tokenNumber`, since the user story's core value ("receive a token number") would otherwise require a second round trip to learn it — caught and fixed during planning, before implementation. `com.cms.booking.BookingExceptionHandler` gained 3 new mappings for `SessionNotFoundException`/`NotAQueueSessionException`/`TokenIssuanceFailedException` — the same "015→016" first-HTTP-caller gap class, avoided proactively via research.md rather than caught reactively. `/speckit-analyze` found 1 LOW (missing appointment-type-mismatch test coverage) and 1 MEDIUM (the detached-entity risk that convergence later found the fix for didn't work) finding — both addressed, though the MEDIUM's fix needed a second pass to actually land correctly. |
| 021-automatic-no-show-detection | Converged | specs/023-no-show-detection | 15/15 tasks complete; full backend build (spotless+compile) and test compilation both green with zero regressions. 6 new integration tests written/compiling but unexecuted (re-confirmed via direct run: `IllegalStateException: Could not find a valid Docker environment`), same sandbox limitation as every prior feature. Two genuine open questions resolved via `/speckit-clarify` (both user-confirmed the recommended default): Fixed-Time-only scope (Queue/Token has no scheduled time to measure a grace period against); and a new currently-unset `Slot.onHold` flag, built now even though no feature in this 39-item backlog yet provides any way to set it — matching this session's established "infrastructure a stated business rule requires, ahead of the feature that will set it" pattern (011's notification opt-in flags). `SlotStatus` extended in place with `NO_SHOW` (mirroring how `BOOKED` itself was added by 020) rather than a new `Booking`-level status, since the spec's own wording consistently describes the *Slot* as what changes state. Deliberately builds no denormalized no-show counter on `Patient` and no new query method for the "queryable per-patient/date" requirement (FR-005) — satisfied structurally by `Slot.status = NO_SHOW` plus `Booking`'s existing associations, left for 022-buffer-slot-capacity-sizing (its actual future consumer) to query however it needs when it's built. New `com.cms.scheduling.NoShowDetectionService`/`NoShowDetectionTrigger` mirror `SessionGenerationService`/`NightlySessionGenerationTrigger`'s existing `@Scheduled` shape, but running every minute (not nightly) given the 10-minute grace period. No HTTP endpoint or frontend (Constitution III's service-interface-only allowance, same shape 015's `FeeResolutionService` used before 016). Designed proactively (not found reactively) to avoid the self-invocation `@Transactional`-bypass bug class this session found twice already (020, 022's first implementation attempt): the sweep has no same-class `@Transactional` helper at all — `slotRepository.save()` is independently atomic on its own. `/speckit-analyze` found 0 issues; convergence found 0 issues on the first pass — this feature's design proactively applied the exact lesson from 020/022, rather than needing a second pass to fix it. |
| 022-buffer-slot-capacity-sizing | Converged | specs/024-buffer-slot-capacity-sizing | 13/13 tasks complete; full backend build (spotless+compile) and test compilation both green with zero regressions. 8 new integration tests written/compiling but unexecuted (re-confirmed via direct run: `IllegalStateException: Could not find a valid Docker environment`), same sandbox limitation as every prior feature. This was largely an "upgrade in place" of 018's already-built seam (`BufferSlotCalculator` interface, `SlotGenerationService`'s existing call site and proven even-distribution formula) — exactly as `backlog/build-order.md`'s own circular-dependency resolution #1 anticipated, now unblocked by 021's real no-show data. One genuine formula ambiguity resolved via `/speckit-clarify` (user confirmed the recommended default): a doctor's no-show rate becomes the buffer percentage *directly* (capped at 20%), not scaled down by an extra 0.20 factor — the scaled reading would have made the risk-based formula barely distinguishable from the flat default even for high-risk doctors. New `RiskBasedBufferSlotCalculator` deliberately lives in `com.cms.booking` (not `com.cms.scheduling`, despite implementing a `com.cms.scheduling`-owned interface) specifically to avoid introducing this codebase's first circular package dependency, since its query needs `BookingRepository` — `com.cms.booking` already depends on `com.cms.scheduling`, never the reverse; marked `@Primary` so Spring resolves it into `SlotGenerationService`'s existing injection point with zero changes there. Reuses (rather than duplicates) `ColdStartBufferSlotCalculator`'s existing "return 1" logic via constructor delegation for the below-5-sample-floor branch. `/speckit-analyze` found 1 LOW finding (missing explicit test for the 90-day window's own boundary exclusion) — remediated before implementation began. Convergence found 0 issues on the first pass. |
| 020-walk-in-priority-insertion | Converged | specs/025-walk-in-priority-insertion | 28/28 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (66/66, incl. 5 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run: `IllegalStateException: Could not find a valid Docker environment`), same as every prior feature this session — 13 new integration tests written/compiling but unexecuted. New `WalkInInsertionService`/`WalkInInsertionController` (`com.cms.booking`) implement the full 3-tier priority search (buffer → no-show-freed → regular-with-override-reason) in one cohesive method rather than the tasks.md-planned 3 incremental extensions, since the tiers share one write-ordering; tasks.md was annotated accordingly. Found and fixed a genuine implementation-time issue before it could become a bug: Hibernate's default flush ordering runs pending inserts before pending deletes, so the tier-2 (no-show-slot reuse) delete-then-insert sequence needed an explicit `bookingRepository.flush()` right after the delete — without it the new Booking's INSERT would hit `uq_booking_slot` against the still-present old row (documented in research.md R2). New nullable `Booking.overrideReason` column (V14) via a second constructor overload, existing 5-arg constructor unchanged for 016/017/018/022's call sites. New `POST /api/v1/clinics/{clinicId}/sessions/{sessionId}/walk-in` endpoint targets the Session (not a specific Slot) since the system, not staff, selects the target Slot. Analyze (pre-implementation) found 0 CRITICAL/HIGH, 2 MEDIUM — both remediated before implementation: FR-007 mobile-validation test coverage gap, and SC-001's tier-internal tie-break was undefined since `findBySession_Id` has no ORDER BY (priority search now sorts by `startTime`). Convergence (first pass) found 1 real HIGH gap: no guard rejected a Queue-mode Session, so its narrow-window OPEN-unbooked Slot (between `QueueSlotService.issueNextSlot` minting it and the Queue booking flow saving a Booking against it) could incorrectly qualify as a tier-3 candidate, letting a walk-in be inserted into a Queue-mode session — out of this feature's own declared Fixed-Time-only scope. Fixed with an explicit mode guard (new `NotAFixedTimeSessionException`, mirroring `StaffQueueBookingService`'s inverse `NotAQueueSessionException` precedent exactly, including its load→authorize→mode-check ordering). Re-verified via a second converge pass, 0 further findings. Ships a frontend form (`WalkInForm.tsx`), consistent with 016/020's precedent. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Spec+Clarify were already done in a prior session (found on resume: complete spec.md with Clarifications section already resolved, requirements checklist just not re-ticked — fixed). Plan: reuses 016/020's `com.cms.booking` module, `FeeResolutionService`, walk-in Patient creation, and `saveAndFlush` race-closure pattern entirely as-is; only new schema is one nullable `Booking.overrideReason` column (V14). Endpoint targets the Session (not a specific Slot) since the system — not staff — selects the target Slot via the priority search. FR-001a's atomic delete-old-no-show-Booking-then-create-new-one is ordered last, after fee/patient resolution succeed, so a failed insertion never touches the original no-show Booking. |
| 023-session-delay-tracking | Converged | specs/026-session-delay-tracking | 30/30 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (72/72, incl. 6 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run), same as every prior feature this session — 8 new integration tests written/compiling but unexecuted. Built the first-ever "mark completed" capability in this codebase: new `SlotCompletionService`/`SessionDelayService` (both `com.cms.scheduling`, not `com.cms.booking` — neither touches a `com.cms.booking`-owned entity, per research.md R1), extending `SlotStatus` with `COMPLETED` and `Session` with a nullable, stored-not-computed `delayMinutes` column (V15). Found and fixed a real bug during implementation before it could ship: `SessionDelayController`/`SessionDelayService.currentDelay` initially didn't filter by `clinicId` at all, meaning any Session ID would resolve regardless of which clinic's token was used — added the same clinic-scoping filter every other endpoint in this codebase already has. Extended 025's already-converged `WalkInInsertionService` in place with one new call site (`sessionDelayService.recalculate`) as the second trigger point — a plain synchronous call, not an event, since both emitters already exist in the same build (research.md R5). Analyze (pre-implementation) found 0 CRITICAL/HIGH, 1 MEDIUM (FR-006 doctor-can-view coverage gap) — remediated before implementation. Convergence found 0 further findings on the first pass. Ships two frontend components (`CompleteSlotButton.tsx`, `DelayIndicator.tsx`), consistent with 016/020/025's precedent. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | No prior feature built any "mark a Slot completed" capability — this feature builds it from scratch. Clarify (2026-09-04) confirmed staff-only authorization (Operations/ClinicAdmin, never Doctor), matching 016/020/025's precedent, not `ScheduleService`'s doctor-inclusive one. Plan: two new services (`SlotCompletionService`, `SessionDelayService`) live in `com.cms.scheduling` (not `com.cms.booking`) since neither touches a `com.cms.booking`-owned entity. Delay is a stored, nullable `Session.delayMinutes` column recalculated only at two trigger points (Slot completion; walk-in insertion) and never recomputed on read — a plain synchronous call added as a new call site in 025's already-converged `WalkInInsertionService`, not an event (both emitters already exist in the same build). |
| 024-queue-position-tracking | Converged | specs/027-queue-position-tracking | 21/21 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (75/75, incl. 3 new) both green with zero regressions. Hit and fixed the known OneDrive `backend/build` corruption once this session. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run), same as every prior feature this session — 6 new integration tests written/compiling but unexecuted. New `QueuePositionService` (`com.cms.booking`, keyed by Booking — the mirror-image placement decision from 023's Session-keyed `SessionDelayService`) computes position live on every call, deliberately NOT stored/cached like 023's delay figure (no staleness requirement exists here). Two thin, separately-secured controllers (staff clinic-scoped, patient ownership-scoped) share one computation, per SC-005's explicit no-drift requirement. Analyze (pre-implementation) found 0 CRITICAL, 1 HIGH (staff endpoint's contract never specified the "caller has zero role at this clinic" 403 case) — remediated before implementation. New role-agnostic `RoleAssignmentRepository.existsByAccount_IdAndClinic_IdAndActiveTrue` query — the first staff-gated read in this codebase with no role exclusion at all. Convergence (first pass) found 1 LOW finding: an unused `QueuePositionService.position(UUID)` overload (dead code — both controllers correctly call `positionOf(Booking)` instead) — removed along with its now-unused `BookingRepository` dependency. Re-verified via a second converge pass, 0 further findings. Ships one shared frontend component (`QueuePositionIndicator.tsx`, mode-switched staff/patient) used by both audiences. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Clarify found no critical ambiguities (all resolved via clear defaults/precedent, documented as Assumptions). Plan: `QueuePositionService` lives in `com.cms.booking` (keyed by Booking, not Session — mirror-image placement decision from 023's Session-keyed `SessionDelayService`). Computed live at every query, deliberately NOT stored/cached like 023's delay figure (no staleness requirement exists here). "Active" currently excludes only COMPLETED/NO_SHOW — no CANCELLED status exists anywhere in this codebase yet (cancellation features are later in build-order.md and will need to extend this exclusion set when they add one). Two thin controllers (staff clinic-scoped, patient ownership-scoped) share one computation, per SC-005's explicit no-drift requirement. |
| 025-individual-booking-cancellation-waitlist-trigger | Converged | specs/028-individual-booking-cancellation | 29/29 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (79/79, incl. 4 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, incl. re-running 016's own pre-existing `StaffBookingAlreadyBookedTest` unmodified to confirm no regression from the schema change), same as every prior feature this session — 11 new integration tests written/compiling but unexecuted. Analyze (pre-implementation) found 0 CRITICAL/HIGH, 1 MEDIUM (would-be redundant double-fetch, the exact shape 027's own convergence pass had just fixed) — remediated before implementation: `BookingCancellationService.cancel` takes an already-fetched `Booking`, mirroring `QueuePositionService.positionOf(Booking)`. Built the first-ever Booking lifecycle status (`BookingStatus.ACTIVE`/`CANCELLED`, independent of `PaymentStatus`) and the first *reverse* Slot transition (`BOOKED → OPEN`) in this codebase — required replacing 016's flat `uq_booking_slot` constraint with a partial index scoped to non-cancelled Bookings (Clarifications, interactive question: retain cancelled Bookings for audit, not delete them like 025-walk-in's no-show-reclaim precedent). Found and fixed two real bugs during implementation before they could ship: (1) the in-memory `Booking` entity would have read stale (still `ACTIVE`) after the data-layer-guarded `cancelIfActive` update, since that `@Modifying` query bypasses the persistence context — added an explicit `Booking.setStatus` sync call; (2) `WalkInInsertionService`'s no-show-reclaim `findBySlot_Id` call (025) would throw `IncorrectResultSizeDataAccessException` once a Slot could carry multiple historical Booking rows — added `findBySlot_IdAndStatus` and switched that one production call site to it; (3) `PatientBookingCancellationController`'s cutoff computation would NPE on a Queue-mode Booking's null `Slot.startTime` if the Fixed-Time-mode check ran after it — reordered. Concurrency race-closure (FR-008) uses a data-layer-guarded conditional `@Modifying` update (`cancelIfActive`), mirroring 036's convergence-fixed `markActioned` pattern, verified via a genuine 10-thread concurrency test with a `@TestConfiguration`-scoped recording event listener (not a bare `@Component`, to avoid polluting every other test's shared Spring context). Ships two frontend components sharing one mode-switched button (`CancelBookingButton.tsx`), consistent with 027's precedent. Convergence found 0 further findings on the first pass. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | No prior feature defines a Waitlist Entry or "join waitlist" action, and 028/029 (matching/claim) are built after this one — resolved by publishing a durable `BookingCancelledEvent` (mirrors 003→008, 036→037's "publish now, consumer built later" precedent) rather than attempting any matching/claim logic itself. Clarify (interactive question, high-impact since it changes a converged table's schema): cancelled Bookings are retained (new `CANCELLED` status), not deleted like 025-walk-in's no-show-reclaim precedent — `uq_booking_slot` becomes a partial unique index scoped to non-cancelled Bookings so the Slot can be rebooked while history is kept. Plan: concurrency race-closure (FR-008) uses a data-layer-guarded conditional `@Modifying` update (mirrors 036's convergence-fixed `markActioned` bug class), not a plain read-then-write — the event only publishes after that update confirms the transition actually happened. One shared, concurrency-critical `BookingCancellationService` behind two thin staff/patient controllers (mirrors 027's shared-computation shape, not 016/017's full-duplication one, since the shared part here is correctness-critical). |
| 026-whole-day-session-cancellation | Converged | specs/029-whole-day-session-cancellation | 19/19 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (82/82, incl. 3 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run), same as every prior feature this session — 7 new integration tests written/compiling but unexecuted. Clarify (interactive question): cancelled Slots return to `OPEN`, mirroring 025's per-Booking transition — no new Session-level "cancelled" concept, keeping the change scoped to `com.cms.booking` alone. Analyze found 0 findings (the one real gap this feature surfaced — FR-005/FR-008's internal inconsistency — was already caught and fixed during planning). Caught and fixed one process gap during implementation: the task breakdown itself omitted a `SecurityConfig` matcher task despite every prior feature needing one — added (`T003a`) and wired before it could become a silently-public endpoint (014's own precedent gap class). Reuses 028's `cancelIfActive` data-layer guard directly (not `BookingCancellationService.cancel`, which always publishes the waitlist-bump event this feature must never fire) — verified via a cross-feature concurrency test racing this action against an individual 025 cancellation on the same Booking. First real production caller of 036's `NotificationEventService.publish` (036/037 were built with no fabricated call-sites, deliberately waiting for a real one). Ships `CancelSessionButton.tsx`, consistent with 025/026/027's precedent. Convergence found 0 findings on the first pass. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Clarify (interactive question): cancelled Slots return to `OPEN`, mirroring 025's per-Booking transition exactly — no new Session-level "cancelled" concept, keeping the change scoped to `com.cms.booking` alone rather than touching every existing booking-selection query. Found and fixed a self-introduced spec inconsistency during planning: FR-005 ("reject if already cancelled") implied detectable Session-level state that FR-008's resolved answer explicitly rejected — resolved by defining "already cancelled" structurally as "zero currently-active Bookings in the Session," needing no new flag. Plan: reuses 025's `cancelIfActive` data-layer guard directly (not `BookingCancellationService.cancel`, which always publishes the waitlist-bump event this feature must never fire) — the two features share the guard primitive, diverge only on the one dimension they must. First real production caller of 036's `NotificationEventService.publish` (036/037 were built with no fabricated call-sites, deliberately waiting for a real one). |
| 027-partial-cutoff-session-cancellation | Converged | specs/030-partial-cutoff-session-cancellation | 17/17 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (85/85, incl. 3 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run), same as every prior feature this session — 7 new integration tests written/compiling but unexecuted. Clarify (2 interactive questions): bare OPEN slots at/after the cutoff are left untouched (mirrors 026, no new SlotStatus) — this corrected a real conflict in the source text; Queue-mode "scheduled time" (which doesn't exist in the actual schema) resolved as `Slot.createdAt`. Analyze found 0 functional findings; explicitly included the SecurityConfig matcher task this time (029's own tasks.md omitted it once). Nearly identical design to 029 (whole-day cancellation), reusing its response DTO, differing on exactly one behavior: a cutoff matching nothing is a normal zero-count success here, not a rejection. Found and fixed one test-infrastructure subtlety during implementation: `Slot.createdAt` has no production setter (by design), so backdating it for Queue-mode cutoff tests needed a raw JDBC update plus an explicit `EntityManager.refresh` to avoid reading a stale, cached value back. Convergence (first pass) found 1 LOW gap: the spec's own Edge Cases section describes a concurrent-cancellation race (partial vs. 025's individual cancellation) that no task actually tested, unlike 029's identical scenario — added and verified via a genuine 2-thread cross-feature concurrency test. Re-verified via a second converge pass, 0 further findings. Ships `CancelFromCutoffForm.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Clarify (2 interactive questions): bare OPEN slots at/after the cutoff are left untouched (mirrors 026, no new SlotStatus needed) — this corrected a real conflict in the source text, which claimed OPEN slots get "cancelled" too but no terminal state exists for that. Queue-mode "scheduled time" (which doesn't exist in the actual schema, despite the source text's claim) resolved as `Slot.createdAt` (token-issuance timestamp). Plan: nearly identical to 026's design, reusing its response DTO — the one deliberate behavioral difference is that a cutoff matching nothing is a normal zero-count success here, not a rejection (026's "already cancelled" semantics don't apply to an ordinary late cutoff choice). |
| 028-waitlist-matching-longest-waiting | Converged | specs/031-waitlist-matching-longest-waiting | 32/32 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (23 files/89 tests, incl. 4 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 6 new test classes), same as every prior feature this session — 6 new integration test classes written/compiling but unexecuted. Materializes the constitution's own named `com.cms.waitlist` module for the first time. Corrected during implementation: `ClinicNotFoundException`/`DoctorNotStaffedAtClinicException`/`ForbiddenException` are reused directly from `com.cms.scheduling` (already globally mapped by `ScheduleExceptionHandler`) rather than duplicated as originally scoped in tasks.md; `PatientAccountNotFoundException` (011) is reused too but got its first-ever HTTP mapping here. Convergence (first pass) found 1 CRITICAL + 1 MEDIUM gap: `WaitlistMatchingService.matchAndOffer` did a plain read-then-write on `WaitlistEntry` with no data-layer race-closure (Constitution IV violation — two concurrent individual cancellations for the same doctor could both offer the same entry) — fixed with a conditional `@Modifying` update (`WaitlistEntryRepository.offerIfWaiting`, mirroring `BookingRepository.cancelIfActive`), lost-race case skipped silently per 026/027's precedent; the exclusivity test suite (T022) only covered 2 of its own 3 scoped release paths (whole-day, partial-cutoff, missing no-show) — added a third test invoking the real `NoShowDetectionService` sweep. Re-verified via a second converge pass, 0 further findings. Ships `JoinWaitlistForm.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | No prior feature defines a WaitlistEntry or "join waitlist" action (same gap 025 flagged and deferred) — resolved by having this feature, the first to actually need entries to exist, also build the join action as a necessary prerequisite (User Story 2, tied P1 with the matching logic itself). First feature to materialize the constitution's own named "waitlist" module. Plan: matching triggers via `@TransactionalEventListener(phase = AFTER_COMMIT)` on 025's `BookingCancelledEvent`, mirroring 037's identical event-consumption pattern — never a direct call, structurally guaranteeing FR-009's "only 025 can trigger this" exclusivity. The 30-minute claim window's expiry is owned by `WaitlistEntry` itself (`offerExpiresAt`), not by the notification pipeline, to avoid a future feature (029) needing to read business state from a module that should stay a pure sink. |
| 029-self-service-waitlist-claim | Converged | specs/032-self-service-waitlist-claim | 29/29 tasks (+T022a/T022b Analyze-driven additions) complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (24 files/93 tests, incl. 8 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 11 `com.cms.waitlist` test classes), same as every prior feature this session — 5 new integration test classes written/compiling but unexecuted. Extends 028's `com.cms.waitlist` module in place (no new module): `WaitlistEntry` gains `offeredSlot`/`CLAIMED`/`EXPIRED`; `WaitlistMatchingService` gains an OPEN-Slot guard (R10) and now persists `offeredSlot`; claim reuses `PatientBookingService.bookSlot` (021) directly rather than reimplementing booking creation; decline and the new `@Scheduled` expiry sweep (mirrors `NoShowDetectionTrigger`, 023) share one `WaitlistReleaseService.release` core. Clarify (1 interactive question): an outstanding offer never reserves the Slot — ordinary booking can still take it, and a claim that loses that race gracefully falls through to the next eligible entry, rather than introducing a new held/reserved Slot state. Analyze (before implementation) found 2 real coverage gaps and both were closed before/alongside implementation: no task exercised an actual concurrent claim/decline/expiry race (added T022b, a genuine 2-thread test) and no task chained more than one hop of FR-008's "repeat until claimed or exhausted" (added T022a, a real 3-entry decline→expire→claim cascade). Found and fixed one genuine bug during implementation, independently confirmed via a dedicated verification pass: the claim-failure pivot (on `SlotAlreadyBookedException`) was originally written as a plain in-line write inside `claim`'s own transaction, but `PatientBookingService.bookSlot`'s own thrown exception already marks that *participating* transaction rollback-only before the catch block runs, and `claim` re-throwing afterward rolls back the whole thing — silently discarding the intended "entry ends EXPIRED, next entry offered" outcome. Fixed by moving that pivot into `WaitlistReleaseService.releaseById`, a separate bean method under `Propagation.REQUIRES_NEW`, documented as research.md R11. Ships `ClaimOfferCard.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I) — the REQUIRES_NEW fix in particular has not been exercised against a real transactional database. | Clarify: the offered Slot is never reserved during the 30-minute window — ordinary booking (016/017/018) can still take it; a claim that loses that race falls through gracefully to the next eligible entry rather than the system ever introducing a new held/reserved Slot state, which would have required touching every existing booking-eligibility check across 4+ prior features. |
| 008-deverification-cascade-auto-cancel-bookings | Converged | specs/033-deverification-cascade-auto-cancel | 21/21 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (24 files/94 tests, incl. 1 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 85 tests in the com.cms.booking/com.cms.waitlist/com.cms.identity.admin packages — the full regression surface, not just this feature's own 2 new test classes), same as every prior feature this session. No new module: consumes `ClinicDeVerifiedEvent` (003), which had existed with no listener since that feature shipped; builds the missing explicit doctor-license-revoke admin action as Trigger 2's necessary prerequisite (`DoctorVerificationService.revoke`, mirroring `unverify`'s exact idempotent shape); places the cascade service/listener in `com.cms.booking` rather than `com.cms.identity.admin`, matching where every other cancellation service already lives. "Not-yet-occurred" resolved via Slot-status (`BOOKED`), not a calendar-date comparison, reusing 021/026's own existing lifecycle state rather than re-deriving a same-day boundary rule. Analyze (before implementation) found 2 minor gaps, both fixed before/alongside implementation: no task asserted re-verification doesn't restore cascade-cancelled bookings (FR-008) — added inline; a task's own wording implied notification only applied to the Queue-mode branch when FR-006 requires it uniformly — reworded. Found and fixed one genuine bug during implementation, independently confirmed via a dedicated verification pass (the same class of bug found in 029's own convergence): `DeVerificationCascadeService`'s batch-cancellation loop called `BookingCancellationService.cancel` (025) from within its own active transaction; a single lost race anywhere in a large batch (an expected, routine outcome) would silently discard every *other* successfully-cancelled booking in that same batch when the shared transaction failed to commit. Fixed by switching `cancel` to `Propagation.REQUIRES_NEW` — verified behaviorally identical for its two existing callers (both non-transactional controllers) — documented as research.md R8. Ships a "Revoke" action on the existing doctor-verification admin UI. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I) — the REQUIRES_NEW fix in particular has not been exercised against a real transactional database. | No "explicitly reject/revoke a doctor's license" action existed anywhere — discovered during pre-intake codebase research (not left to Clarify) and resolved via a well-precedented Assumption: build it as the minimal necessary prerequisite, mirroring `ClinicVerificationService.unverify`'s exact shape rather than overloading the existing automatic-reset `edit` action. `BookingCancellationService.cancel` (025) is Fixed-Time-only and can't be reused as-is for Queue-mode bookings — resolved via Assumption (not Clarify) as a Fixed-Time-uses-025-directly / Queue-mode-uses-029/030's-own-direct-cancelIfActive-pattern split, since the spec's own text already implied both modes are cancelled, just with different consequences. |
| 030-consultation-note-creation | Converged | specs/034-consultation-note-creation | 20/20 tasks (+T012a Analyze-driven addition) complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (25 files/98 tests, incl. 4 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across both new `com.cms.clinical` test classes), same as every prior feature this session. First feature to materialize the constitution's own named "clinical documentation" module (`com.cms.clinical`) — no new module dependency reversal, reads only from `com.cms.booking`/`com.cms.identity`. Immutability is structural (no update/delete code exists anywhere, not merely unauthorized); one-note-per-booking is a genuine data-layer `UNIQUE` constraint, closed via the established `saveAndFlush` + catch-`DataIntegrityViolationException` race-closure shape (021's own precedent) — verified via a real 2-thread concurrency test, exactly one winner. Analyze (before implementation) found 1 gap: FR-007 explicitly forbids any booking/slot-status precondition on note creation, but no task tested that creation actually succeeds against a `CANCELLED`/`COMPLETED`/`NO_SHOW` booking — added T012a as a regression guard before implementing. Reviewed for the same transaction-rollback-poisoning bug class found and fixed twice earlier this session (029, 033) — confirmed safe by inspection: `create()` re-throws immediately after catching the constraint violation with no further work in between, so the transaction owner performs a clean rollback rather than risking `UnexpectedRollbackException`, unlike the two prior bugs (both of which caught-and-continued before returning normally). Ships `ConsultationNoteForm.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | No prior feature built any note-reading/retrieval capability, and the acceptance criteria only described writing — resolved via a well-justified Assumption during Specify (not Clarify): a permanent clinical record that can never be read back doesn't meaningfully serve its own stated purpose, so a minimal treating-doctor-only `GET` was included as an obviously-necessary minimum, not scope creep. The source material's own explicitness (immutability, one-note-per-booking, authorization tracing all stated directly) meant zero interactive clarification questions were needed for this feature — the first this session with none at all. |
| 031-prescription-and-items-creation | Converged | specs/035-prescription-and-items-creation | 24/24 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (26 files/102 tests, incl. 4 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 4 `com.cms.clinical` test classes — the 2 new ones and 030's own 2, re-run as a regression check), same as every prior feature this session. Extends 030's `com.cms.clinical` module in place. Extracted `TreatingDoctorAuthorizationService` from 030's `ConsultationNoteService` now that a second real caller needed the identical booking-lookup + doctor-identity-trace logic — a behavior-preserving refactor of already-converged code, verified via 030's own existing test classes re-run unmodified. One deliberate cardinality difference from its sibling: no uniqueness constraint on `prescription.booking_id` — a booking may carry any number of independent Prescriptions, unlike Consultation Notes' data-layer-enforced one-per-booking — so, unlike 030, no concurrency race-closure was needed here at all (nothing to protect). Reviewed for the same transaction-rollback-poisoning bug class found and fixed twice earlier this session (029, 033) and confirmed inapplicable — `PrescriptionService.create` has no participating-transactional-call-with-caught-exception shape at all (a plain `save`, no uniqueness violation possible). Ships `PrescriptionForm.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | This feature's own source material explicitly flagged the shared-authorization-extraction opportunity with 030 in its Dependencies section, and the intake note made the case concretely (a second real caller now exists) rather than speculatively — resolved as: extract now, refactor 030's already-converged service to use it, verify via 030's own existing tests. Zero interactive clarification questions needed, the second feature this session with none (030 was the first) — the source material's own explicit "unlike Consultation Notes, zero-or-more per booking" framing resolved the one real fork directly. |
| 032-external-record-reference | Converged | specs/036-external-record-reference | 19/19 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (27 files/105 tests, incl. 3 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 6 `com.cms.clinical` tests run — the 2 new classes plus 030/031's own 4, re-run as a regression check), same as every prior feature this session. Third and final feature completing the constitution's own named clinical-documentation module trio (030, 031, 032) — extends `com.cms.clinical` in place, makes zero edits to either sibling's code, and reuses `TreatingDoctorAuthorizationService` (031's extraction) unchanged as its third caller. Flat entity, no child collection (unlike 031's Items) — a single typed-summary record with no file/document field of any kind, matching this system's own system-wide no-upload-capability boundary. Same zero-or-more-per-booking cardinality as 031, no data-layer race to close (nothing to protect, mirrors 031's own reasoning). Analyze found 0 gaps — full FR coverage on the first pass, the smoothest of the three siblings. Ships `ExternalRecordReferenceForm.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Third of three clinical-documentation siblings with zero interactive clarification questions needed — the source material's own explicit field list and cardinality statement ("mirrors 031, not 030") resolved the only two genuine forks directly, without even needing to re-derive the reasoning 030/031 had already established. |
| 033-patient-immediate-anonymization | Converged | specs/037-patient-immediate-anonymization | 20/20 tasks complete; full backend build (compile + spotless, green via `-x test`) and full frontend suite (28 files/109 tests, incl. 4 new) both green with zero regressions. Backend test execution blocked by the sandbox's Testcontainers/Docker limitation (confirmed via direct run, identical `IllegalStateException` across all 11 `com.cms.patient.record` tests — the 2 new classes plus 009's own 9, confirming no regressions from extending `Patient`), same as every prior feature this session. First DPDP-compliance feature — no new module; lives entirely in `com.cms.patient.record` (which already owns `Patient`), including its own controller (corrected mid-plan: initially drafted for `com.cms.identity.admin`, then caught that package is Super-Admin-Basic-Auth-only, a different mechanism from this feature's ordinary staff-JWT Operations-or-ClinicAdmin gate — moved before implementation). Only the two identifying fields the actual `Patient` entity has (`name`, `phone`) are scrubbed — the source material's broader illustrative field list (address, emergency contact, safety-form text) doesn't exist anywhere in this codebase and wasn't invented to match it. `anonymizedAt` added as the durable marker 034's own future precondition check will need. "Active future booking" reuses 033/deverification-cascade's own Slot-status-based definition, scoped by patient. Analyze surfaced one documented, accepted limitation (not fixed): the block-check itself isn't raced against an independent, concurrent booking-creation transaction — a narrow, low-likelihood window with no stated requirement to close it, distinct from the (correctly reasoned as harmless) anonymize-vs-anonymize race. Reviewed for the same transaction-rollback-poisoning bug class found and fixed twice earlier this session (029, 033) — confirmed inapplicable, no participating-transactional-call-with-caught-exception shape exists here. Ships `AnonymizePatientButton.tsx`. Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | Zero interactive clarification questions — all three genuine forks discovered during intake research (the actual, narrower Patient field list; the need for a durable anonymized marker; module/controller placement) were resolved via well-precedented Assumptions, the placement one caught and corrected mid-plan rather than left wrong. |
| 034-monthly-retention-purge | Converged | specs/038-monthly-retention-purge | 21/21 tasks complete; full backend `compileJava`/`compileTestJava` green, zero regressions. Full backend test run (226 tests, incl. 11 new: `RetentionPurgeTest` x7, `RetentionPurgeAuthorizationTest` x4) shows only the sandbox's known Testcontainers/Docker limitation (`IllegalStateException`, confirmed by grepping every result XML for the failure signature — 0 non-Docker failures), same as every prior feature this session. Second DPDP-compliance feature — no new module; purge service lives in `com.cms.clinical` (owns the deleted content, already reads `Booking` directly per 030/031/032), reading `Patient.anonymizedAt` via `Booking.getPatient()` (no new module dependency edge, matches the existing `clinical → booking → patient.record` transitive shape). Retention clock anchored to `Booking.createdAt` uniformly across both scheduling modes (a coarse, multi-year window makes the mode-specific time-proxy fork 027/030 needed for a finer-grained concern unnecessary here). First real, foreseen exception to 031's own "no update/delete code path exists anywhere in this module" claim: `Prescription.items`' cascade extended from `PERSIST`-only to include `REMOVE` so deleting a `Prescription` correctly cascades to its `PrescriptionItem` rows — minimal and behavior-preserving for 031's own existing scope (031 itself never calls delete). Controller placement is the mirror image of 033/037's own correction: this feature's manual trigger genuinely IS Super-Admin-gated per its own business rule, so `com.cms.identity.admin` (behind the existing `SuperAdminSecurityConfig` Basic-Auth chain) is the *correct* home here, reasoned through explicitly rather than pattern-matched against either prior placement case. No new domain event (Constitution II) — a pure batch sweep with no cross-module side effect beyond the deletions themselves. No frontend changes — this feature is backend-only (a background job plus a Super Admin operational endpoint, no staff/patient-facing UI). Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). | The retention-date-anchor question (which timestamp starts the 3-year clock) was elevated above this session's usual "resolve silently via Assumption" bar given its compliance stakes, per auto-mode guidance to make the reasonable call and proceed rather than pause for interactive clarification — resolved to `Booking.createdAt`, documented as an Assumption in spec.md rather than a `[NEEDS CLARIFICATION]` marker. Zero other ambiguities: the source material's own business rules (fixed 3-year constant, both-conditions-required, booking/Patient-shell-never-purged, Super-Admin-only manual trigger) were explicit enough that no interactive clarification questions were needed at all. |
| 038-unified-realtime-inbox | Converged | specs/039-unified-realtime-inbox | Spec drafted covering view/claim/resolve lifecycle across walk-in (020), waitlist-offer (029), and de-verification-cascade (008) item types. Zero NEEDS CLARIFICATION markers at Specify — the one genuine ambiguity (walk-in item = pre-insertion queue vs. post-insertion coordination notice) resolved via Assumption per Constitution II (Simplicity/YAGNI). Clarify (1 interactive question): a real DPDP gap this spec hadn't accounted for — Inbox Items must derive patient-identifying summary content live from the referenced entity (FR-016), so 033's anonymization/034's purge propagate automatically with no separate scrubbing logic. Plan: new `com.cms.inbox` module (pure sink, mirrors `com.cms.notification`); real-time delivery via Spring's built-in `SseEmitter` (zero new dependency) consumed frontend-side via `fetch` streaming rather than native `EventSource`, since EventSource can't carry the existing Authorization header and a query-param-token fallback would leak the staff JWT into access logs; claim/release/resolve use the same data-layer-guarded conditional-`@Modifying`-update race-closure pattern proven three times already this session; `booking_id`/`waitlist_entry_id` FKs are `ON DELETE CASCADE` so 034's future purge needs zero Inbox-specific code. Constitution Check: PASS, no violations, no foundational-stack flag needed (project stack already established by 001). Tasks: 46 tasks across 3 user stories (US1 view-feed, US2 claim, US3 resolve). Analyze: 0 CRITICAL, 1 HIGH, 1 MEDIUM, 2 LOW — both HIGH/MEDIUM remediated pre-implementation: F1, the FR-013 waitlist-auto-resolve path was spec'd as a plain application-level check instead of this session's own proven data-layer-guarded conditional-update pattern (a real race against a concurrent staff `resolve`) — added `resolveIfNotResolved` plus a new concurrency test (T035a); F2, data-model.md incorrectly described 032's re-offer cycle as creating a new WaitlistEntry row (it re-offers the next existing longest-waiting entry) — corrected, no code impact. Implement: 46/46 tasks complete. New `com.cms.inbox` module (the last new module this session) — first genuine two-way inter-module dependency in this codebase (inbox reads live `Booking`/`WaitlistEntry` relations for FR-016's anonymization-propagation guarantee, while 020/031/008 call into it directly to create items), accepted and documented as a deliberate exception (research.md R9) since, unlike 022's `RiskBasedBufferSlotCalculator`, this feature has no real one-way-preserving alternative — an Inbox is definitionally a hub. First real use of Spring MVC's `SseEmitter` for push delivery (zero new dependency); frontend consumes it via `fetch` streaming rather than native `EventSource`, since EventSource can't carry the existing Authorization header. Found and fixed two real bugs during implementation, before any test ran: (1) `PatientAccount` has no `name` field anywhere in this system (the same gap 021 already hit) — the waitlist-offer summary was redesigned around `patientContact` (email) instead of a nonexistent `patientName`, with `contracts/inbox.md` corrected; (2) the FR-013 auto-resolve call in `WaitlistClaimService.claim` was originally placed immediately after `entry.markClaimed()`, which would have let a claim that later pivots to `waitlistReleaseService.releaseById`'s independent `REQUIRES_NEW` transaction leave the Inbox Item wrongly stuck `RESOLVED` — the same rollback-poisoning bug class 029/033 already found this session; moved to fire only after the booking actually succeeds. Full backend build (compile + spotless) green; 3 new dependency-free unit tests (`InboxBroadcastServiceTest`) actually execute and pass in this sandbox (a first — every other test in this feature and every prior feature this session is Docker-blocked); all 17 new Testcontainers-backed integration tests compile and fail only with the documented `IllegalStateException: Could not find a valid Docker environment`, re-confirmed this session. Full frontend suite 116/116 green (109 pre-existing + 7 new), lint clean for new files, `tsc -b` clean for new files. Converge found 0 actionable findings on the first pass — every FR/SC/AC/edge-case/constitution-principle traces to implemented, correct code. **This was the 39th and final feature in the backlog — all 39 features are now Converged.** Re-verify all unexecuted tests in a real dev/CI environment before treating as fully constitution-compliant (Principle I). |
