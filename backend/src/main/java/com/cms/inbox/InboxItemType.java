package com.cms.inbox;

/** 038: extensible in place for future item types, mirroring {@code SlotStatus}'s "define only what's needed now" precedent. */
public enum InboxItemType {
    WALK_IN,
    WAITLIST_OFFER,
    DEVERIFICATION_CASCADE
}
